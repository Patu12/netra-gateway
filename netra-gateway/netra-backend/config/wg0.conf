[Interface]
PrivateKey = gBwCmCB+Y7IjT5Tqr72qcxgHJwzG+nG1pSvMryQPL3I=
Address = 10.0.0.1/24
ListenPort = 51820
DNS = 8.8.8.8, 1.1.1.1

# Windows IP forwarding via netsh (enabled on startup, disabled on shutdown)
PostUp = powershell -Command "Set-NetIPForwarding -InterfaceAlias 'wg0' -Enable"
PostDown = powershell -Command "Set-NetIPForwarding -InterfaceAlias 'wg0' -Disable"

# Note: Tailscale runs separately as its own app, handling internet traffic
# Mobile users connect via WireGuard to this PC, and traffic routes out through Tailscale
