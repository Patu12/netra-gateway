services:
  - type: web
    name: netra-backend
    env: node
    region: oregon
    rootDir: netra-backend
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
