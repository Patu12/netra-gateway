services:
  - type: web
    name: netra-backend
    runtime: node
    region: oregon
    rootDir: netra-backend
    buildCommand: npm install
    startCommand: node --max-old-space-size=512 src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
